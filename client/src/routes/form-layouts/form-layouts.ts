import { customElement, FoundationElement } from '@genesislcap/web-core';
import { formLayoutsTemplate as template } from './form-layouts.template';

@customElement({
  name: 'form-layout',
  template
})
export class FormLayouts extends FoundationElement {

}
